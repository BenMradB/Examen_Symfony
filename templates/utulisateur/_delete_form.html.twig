<form method="post" action="{{ path('app_utulisateur_delete', {'id': utulisateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utulisateur.id) }}">
    <button class="btn">Delete</button>
</form>
